# Configuración de Netlify para Projectrack

# Redirección de API al backend en Google Cloud
[[redirects]]
  from = "/api/*"
  to = "http://35.208.217.192:3000/:splat"
  status = 200
  force = true

# Redirección para SPA (Angular Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
